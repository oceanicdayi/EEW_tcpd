# TCPD.C 程式碼審查報告

**審查日期**: 2025-12-12  
**審查範圍**: tcpd.c (3009 行)  
**嚴重程度分級**: 🔴 嚴重 | 🟠 中等 | 🟡 輕微 | 🔵 建議

---

## 📋 問題摘要

| 嚴重程度 | 數量 | 類別 |
|----------|------|------|
| 🔴 嚴重 | 5 | 記憶體洩漏、陣列越界、除零錯誤 |
| 🟠 中等 | 8 | 檔案處理、緩衝區溢位風險、未初始化變數 |
| 🟡 輕微 | 6 | 程式碼品質、效能問題 |
| 🔵 建議 | 5 | 最佳實踐建議 |

---

## 🔴 嚴重問題 (Critical)

### 1. 記憶體洩漏 - out_ss 未釋放
**位置**: 第 218-223 行  
**問題**: `out_ss` 在 `main()` 中分配但從未釋放

```c
// 第 218-223 行
out_ss = (char**) malloc(sizeof(char*)*20);
for(i=0;i<20;i++)
{
    out_ss[i] = (char*) malloc(sizeof(char)*25);
}   
```

**影響**: 程式長時間運行時會累積記憶體洩漏  
**修復建議**: 在程式結束前或 `while(1)` 迴圈外添加釋放邏輯

```c
// 程式結束前
for(i=0; i<20; i++) {
    free(out_ss[i]);
}
free(out_ss);
```

---

### 2. 陣列越界風險 - Pd 陣列存取
**位置**: 第 473 行、第 1943 行  
**問題**: `upd_sec` 可能超過 `Pd[15]` 陣列邊界

```c
// 第 473 行
upd_sec = atoi(out_ss[13]);
ptr[j].Pd[upd_sec]  = atof(out_ss[8]);  // 若 upd_sec >= 15 則越界!

// 第 1943 行
ind = (int) ( floor(vsn_ntri[k].P_S_time) );
vsn_ntri[k].Pd[0] = vsn_ntri[k].Pd[ind];  // 若 ind >= 15 或 < 0 則越界!
```

**影響**: 可能導致程式崩潰或資料損毀  
**修復建議**:

```c
// 第 473 行修復
upd_sec = atoi(out_ss[13]);
if (upd_sec < 0) upd_sec = 0;
if (upd_sec >= 15) upd_sec = 14;
ptr[j].Pd[upd_sec] = atof(out_ss[8]);

// 第 1941-1943 行修復
ind = (int)(floor(vsn_ntri[k].P_S_time));
if (ind < 2) ind = 2;
if (ind >= 15) ind = 14;
vsn_ntri[k].Pd[0] = vsn_ntri[k].Pd[ind];
```

---

### 3. 除零錯誤風險 - 定位演算法
**位置**: 第 1284-1290 行  
**問題**: 當 `dis` 或 `xc` 為零時會發生除零錯誤

```c
// 第 1284-1289 行
xc=(dis*dis-2.*v0/vg*z0-z0*z0) /(2. * dis);  // dis=0 時除零
zc=-1.*v0/vg;                                  // vg=0 時除零
ang1=atan((z0-zc)/xc);                         // xc=0 時問題
ang2=atan(-1.*zc / (dis-xc) );                 // dis==xc 時除零
```

**影響**: 程式崩潰或產生 NaN/Inf 值  
**修復建議**:

```c
// 確保 dis 不為零 (已有 +0.000000001 但不夠)
if (dis < 0.001) dis = 0.001;
xc=(dis*dis-2.*v0/vg*z0-z0*z0) /(2. * dis);

// 檢查 xc
if (fabs(xc) < 0.0001) xc = 0.0001;
ang1=atan((z0-zc)/xc);

// 檢查 dis-xc
double denom = dis - xc;
if (fabs(denom) < 0.0001) denom = 0.0001;
ang2=atan(-1.*zc / denom);
```

---

### 4. 除零錯誤 - 規模計算中的標準差
**位置**: 第 2856 行  
**問題**: `cal_z()` 函數在 `std=0` 時會除零

```c
// 第 2850-2857 行
double cal_z(double x, double avg, double std)
{
    double z=0.0;
    
    if(avg==0.0) return 0.0;  // 只檢查 avg，未檢查 std!
    
    z = fabs(x-avg)/std;      // std=0 時除零!
    return z;
}
```

**修復建議**:

```c
double cal_z(double x, double avg, double std)
{
    double z=0.0;
    
    if(avg==0.0 || std < 0.0001) return 0.0;  // 同時檢查 std
    
    z = fabs(x-avg)/std;
    return z;
}
```

---

### 5. 檔案處理錯誤 - 未關閉檔案
**位置**: 第 189-214 行  
**問題**: 在某些錯誤路徑中檔案未正確關閉

```c
// 第 189-214 行
fp=fopen("num_eew_status","r");
if(fp==NULL)
{
    // ... 處理 NULL 情況
}
else
{
    fgets(tmp,99,fp);
    if( strlen(tmp)>4 || atoi(tmp) < 1 || atoi(tmp) > 10000 )
    {	
        num_eew = 1;
        fp=fopen("num_eew_status","w");  // 重新開啟但未先關閉舊檔案!
        fprintf(fp,"%d", num_eew);
        fclose(fp);		
    }
    else
    {
        num_eew = atoi(tmp);
        fclose(fp);
    }
}
```

**修復建議**:

```c
fp=fopen("num_eew_status","r");
if(fp==NULL)
{
    // ...
}
else
{
    fgets(tmp,99,fp);
    fclose(fp);  // 先關閉讀取模式的檔案
    
    if( strlen(tmp)>4 || atoi(tmp) < 1 || atoi(tmp) > 10000 )
    {	
        num_eew = 1;
        fp=fopen("num_eew_status","w");
        if(fp != NULL) {
            fprintf(fp,"%d", num_eew);
            fclose(fp);
        }
    }
    else
    {
        num_eew = atoi(tmp);
    }
}
```

---

## 🟠 中等問題 (Medium)

### 6. 緩衝區溢位風險 - split_c 函數
**位置**: 第 2598-2631 行  
**問題**: 未檢查 `j` 和 `k` 是否超過分配的陣列大小

```c
// out_ss 分配: 20 個指標，每個 25 字元
out_ss = (char**) malloc(sizeof(char*)*20);
out_ss[i] = (char*) malloc(sizeof(char)*25);

// 第 2613 行
out_ss[j][k]=in_ss[i];  // j >= 20 或 k >= 25 時溢位!
```

**修復建議**:

```c
int split_c(char **out_ss, char *in_ss)
{
    int i,j,k,len;
    len = strlen(in_ss);
    
    j=0;
    k=0;
    for(i=0;i<len;i++)
    {	
        if(isalpha(in_ss[i]) || isdigit(in_ss[i]) || in_ss[i]=='.' || in_ss[i]=='-')
        {
            if(k < 24) {  // 檢查 k 邊界
                out_ss[j][k]=in_ss[i];	
                k++;
            }
        }
        else
        {
            out_ss[j][k]='\0';
            if(j < 19) {  // 檢查 j 邊界
                j++;
            }
            k=0;
        }		
    }
    out_ss[j][k]='\0';
    return j+1;
}
```

---

### 7. 緩衝區溢位風險 - sprintf 使用
**位置**: 多處 (如第 462-465 行)  
**問題**: 使用 `sprintf` 而非 `snprintf`

```c
// 第 462-465 行 - stn_name 等陣列只有 8 字元
sprintf(ptr[j].stn_name,"%s",out_ss[0]);  // 若 out_ss[0] > 7 字元則溢位
sprintf(ptr[j].stn_Comp,"%s",out_ss[1]);
sprintf(ptr[j].stn_Net ,"%s",out_ss[2]);
sprintf(ptr[j].stn_Loc ,"%s",out_ss[3]);
```

**修復建議**:

```c
snprintf(ptr[j].stn_name, sizeof(ptr[j].stn_name), "%s", out_ss[0]);
snprintf(ptr[j].stn_Comp, sizeof(ptr[j].stn_Comp), "%s", out_ss[1]);
snprintf(ptr[j].stn_Net,  sizeof(ptr[j].stn_Net),  "%s", out_ss[2]);
snprintf(ptr[j].stn_Loc,  sizeof(ptr[j].stn_Loc),  "%s", out_ss[3]);
```

---

### 8. 陣列越界風險 - x 陣列中的 kk 索引
**位置**: 第 1309-1322 行  
**問題**: `kk` 從 `nsta` 開始但未更新，導致寫入同一位置

```c
// 第 1309-1322 行
kk=nsta;
for(i=0;i<nsta;i++)
{
    for(j=0;j<nsta;j++)
    {
        if(i!=j)
        {
            x[0][kk]=x[0][i]-x[0][j];  // kk 從未遞增!
            x[1][kk]=x[1][i]-x[1][j];  // 應該是 kk++?
            x[2][kk]=x[2][i]-x[2][j];
            x[3][kk]=0.0;
        }
    }
}
```

**疑問**: 這可能是故意的設計也可能是 bug，需要確認邏輯  
**建議**: 確認是否需要 `kk++`

---

### 9. 未初始化變數風險
**位置**: 多處  
**問題**: 區域變數可能在使用前未正確初始化

```c
// 第 1155 行 - locaeq 中的陣列
double x[4][1000],y[4],b[4][4],a[4][4],c[4],trv[1000],dist[1000],ngap[1000];
// 這些陣列未初始化

// 第 1869 行 - Magnitude 中
int nntc=0, i, k, j, l, vv=0, dd=0, tt=0, bb=0;
// l 已宣告但從未使用
```

---

### 10. fgets 未檢查返回值
**位置**: 第 200 行  
**問題**: `fgets` 可能失敗返回 NULL

```c
fgets(tmp,99,fp);  // 未檢查返回值
if( strlen(tmp)>4 ... )  // 若 fgets 失敗，tmp 內容不確定
```

**修復建議**:

```c
if(fgets(tmp,99,fp) == NULL) {
    num_eew = 1;
    fclose(fp);
    // 處理錯誤
}
else if( strlen(tmp)>4 || atoi(tmp) < 1 || atoi(tmp) > 10000 )
{
    // ...
}
```

---

### 11. atoi/atof 錯誤處理不足
**位置**: 多處  
**問題**: 使用 `atoi`/`atof` 無法區分 "0" 和轉換錯誤

```c
// 第 467 行
upd_sec = atoi(out_ss[13]);  // 若 out_ss[13] 不是數字，返回 0
```

**建議**: 使用 `strtol`/`strtod` 並檢查錯誤

---

### 12. log 參數可能為負或零
**位置**: 第 1290, 2672, 2684 等多處  
**問題**: `log10()` 和 `log()` 的參數可能為負或零

```c
// 第 1290 行
ptime=(-1./vg)*log(fabs(tan(ang2/2.)/tan(ang1/2.)));
// 若 tan(ang1/2) 為零，則參數為 inf

// 第 2672 行
ans = 5.000 + 1.102 * log10(pd) + 1.737 * log10(dis);
// 雖然有 if(pd<=0) 檢查，但 dis 也需要檢查
```

---

### 13. Magnitude 函數可能未釋放記憶體即返回
**位置**: 第 1867-2177 行  
**問題**: 在某些 return 路徑中未釋放所有分配的記憶體

```c
// 第 2000 行
free(Mpd);  free(Mtc);
free(zMpd); free(zMtc);
return;       

// 但在第 2101-2105 行又分配了新的記憶體
Theo_pa_ratio = (double*) malloc(1000*sizeof(double));
Padj_wei = (double*) malloc(1000*sizeof(double));
...

// 若 2000 行之後的程式碼提前 return，這些不會被釋放
```

---

## 🟡 輕微問題 (Minor)

### 14. 魔術數字
**位置**: 多處  
**問題**: 使用硬編碼數字而非定義常數

```c
#define number 1000  // 好的
// 但仍有很多未定義的:
out_ss = (char**) malloc(sizeof(char*)*20);  // 20 應為常數
out_ss[i] = (char*) malloc(sizeof(char)*25); // 25 應為常數
if(num_split!=14)  // 14 應為常數
```

---

### 15. 變數遮蔽
**位置**: 第 133, 158 行  
**問題**: 區域變數 `fp` 遮蔽全域變數

```c
// 全域 (第 133 行)
FILE *fp;

// main 中區域 (第 158 行)
FILE *fp;  // 遮蔽全域變數
```

---

### 16. 未使用的變數
**位置**: 多處  

```c
// 第 136 行
int rep_fin=0;  // 被使用

// 第 1869 行  
int l=0;  // 從未使用
```

---

### 17. 條件判斷效率
**位置**: 第 201 行  
**問題**: `atoi(tmp)` 被呼叫兩次

```c
if( strlen(tmp)>4 || atoi(tmp) < 1 || atoi(tmp) > 10000 )
```

**建議**:

```c
int val = atoi(tmp);
if( strlen(tmp)>4 || val < 1 || val > 10000 )
```

---

### 18. 無效的 return 語句
**位置**: 第 291, 296, 429 行等  
**問題**: 在 `main()` 中使用不帶值的 `return`

```c
// main 返回類型是 int (第 150 行寫的是 main 但未聲明返回類型)
if(type_error==-2)
{
    printf("...");
    return;  // 應為 return -1; 或 exit(-1);
}
```

---

### 19. main 函數無返回類型
**位置**: 第 150 行  
**問題**: C99 標準要求 `main` 返回 `int`

```c
main( int argc, char **argv )  // 缺少 int 返回類型
```

**修復建議**:

```c
int main( int argc, char **argv )
```

---

## 🔵 建議改進 (Suggestions)

### 20. 缺少 NULL 檢查
**位置**: 第 218, 222 行  
**建議**: malloc 後應檢查是否成功

```c
out_ss = (char**) malloc(sizeof(char*)*20);
if (out_ss == NULL) {
    logit("e", "Memory allocation failed\n");
    exit(-1);
}
```

---

### 21. 函數過長
**位置**: `main()` 約 600 行、`locaeq()` 約 450 行  
**建議**: 將大函數拆分為更小的功能單元

---

### 22. 錯誤訊息中的錯字
**位置**: 第 196, 207 行  

```c
printf("Does not exist ! Creating num_eew_ststus file .... \n");
//                                       ^^^^^^^ 應為 status
printf("Wrong File ! Creating num_eew_ststus file .... \n");
```

---

### 23. 建議使用 const
**位置**: 多處函數參數  
**建議**: 不會修改的指標參數應使用 `const`

```c
// 原始
int split_c(char **out_ss, char *in_ss)

// 建議
int split_c(char **out_ss, const char *in_ss)
```

---

### 24. 平台相容性
**位置**: 第 5 行 (locate.h)  
**問題**: `#include <sys/timeb.h>` 在某些平台可能不可用

---

## 📊 風險評估矩陣

| 問題編號 | 類型 | 發生機率 | 影響程度 | 風險等級 |
|----------|------|----------|----------|----------|
| #1 | 記憶體洩漏 | 確定 | 中 | 中高 |
| #2 | 陣列越界 | 中 | 高 | 高 |
| #3 | 除零錯誤 | 低 | 高 | 中 |
| #4 | 除零錯誤 | 中 | 高 | 高 |
| #5 | 資源洩漏 | 確定 | 低 | 低 |
| #6 | 緩衝區溢位 | 低 | 高 | 中 |
| #7 | 緩衝區溢位 | 低 | 高 | 中 |

---

## ✅ 優先修復順序

1. **立即修復** (Critical):
   - #2 Pd 陣列越界檢查
   - #4 cal_z 除零保護
   - #3 定位演算法除零保護

2. **短期修復** (High):
   - #5 檔案處理邏輯
   - #6, #7 緩衝區邊界檢查
   - #1 記憶體釋放

3. **中期改進** (Medium):
   - #8-#13 其他中等問題

4. **長期重構** (Low):
   - 程式碼品質改進
   - 函數拆分與模組化

---

## 📝 附錄: 修復範例程式碼

見各問題項目中的「修復建議」區塊。
